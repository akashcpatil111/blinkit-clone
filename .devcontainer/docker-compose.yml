version: '3.8'

services:
  devcontainer:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ../..:/workspaces/blinkit-clone:cached
    command: sleep infinity
    # Connect to the same network as the other services
    network_mode: service:mongo
